# 热量营养检测 - 部署到GitHub Pages指南

## 第一步：创建GitHub仓库

1. 登录 [GitHub](https://github.com)
2. 点击右上角 **+** → **New repository**
3. 仓库名填：`calorie-tracker`（或任意名字）
4. 选择 **Public**（公开）
5. 勾选 **Add a README file**
6. 点击 **Create repository**

## 第二步：上传 index.html

### 方法A：网页上传（最简单）
1. 在仓库页面点击 **Add file** → **Upload files**
2. 把桌面 `热量营养检测` 文件夹里的 `index.html` 和 `sw.js` 拖进去
3. 填写提交信息，点 **Commit changes**

### 方法B：用 Git 命令
```bash
git clone https://github.com/你的用户名/calorie-tracker.git
cd calorie-tracker
# 把 index.html 和 sw.js 复制到这里
git add index.html sw.js
git commit -m "添加热量营养检测应用"
git push origin main
```

## 第三步：开启 GitHub Pages

1. 进入仓库 → **Settings** → 左侧菜单 **Pages**
2. **Source** 选择 **Deploy from a branch**
3. **Branch** 选 `main`，文件夹选 `/ (root)`
4. 点击 **Save**
5. 等待 1-2 分钟，页面顶部会显示你的网址：
   ```
   https://你的用户名.github.io/calorie-tracker/
   ```

## 第四步：添加到 iPhone 桌面

1. 在 iPhone 上用 **Safari** 打开上面的网址
2. 点击底部 **分享按钮**（方框+向上箭头）
3. 滚动找到 **"添加到主屏幕"**
4. 点击右上角 **"添加"**
5. 桌面上就出现了「热量营养检测」App图标 🎉
6. 点开就是全屏原生App体验

## 第五步：配置 AI 拍照识别（可选但推荐）

1. 打开 App → 设置 → AI识别设置
2. 点击 **"→ 免费注册百度AI"** 链接注册百度智能云账号
3. 登录 [百度智能云控制台](https://console.bce.baidu.com/ai/#/ai/imageclassify/overview/index)
4. 人工智能 → 图像识别 → 创建应用
5. 勾选 **"菜品识别"**，创建后获取 **API Key** 和 **Secret Key**
6. 复制两个 Key 粘贴到 App 设置页面对应输入框
7. 保存后即可使用 AI 拍照识别食物功能
8. 免费额度：每天 500 次识别

> 不配置也能用！可以通过「手动搜索添加」功能从食物数据库中搜索添加。

## 功能说明

| 功能 | 说明 |
|------|------|
| BMI计算 | 在设置填写身高体重，首页显示5级BMI状态（瘦→偏瘦→正常→偏胖→胖） |
| AI拍照识别 | 拍照后使用百度AI菜品识别自动判断食物，输入重量自动算热量 |
| 手动搜索添加 | 从110+种食物数据库中按分类搜索添加 |
| 热量追踪 | SVG进度环直观展示今日摄入/推荐热量 |
| 缺口/超标提示 | 摄入低于推荐=热量缺口（绿），超出=超标（红） |
| 营养素分析 | 蛋白质/碳水/脂肪进度条，根据目标动态计算每日推荐量 |
| 三Tab布局 | 首页（仪表盘）/ 历史（记录详情）/ 设置（个人信息） |
| 历史记录页 | 日期切换、餐次分组食物列表、清空当日 |
| 日历历史记录 | 月历视图查看任意日期的食物记录和照片 |
| 本周趋势图 | 柱状图展示近7天每日摄入趋势 |
| 历史统计 | 本周日均热量、连续记录天数、总记录天数、最常吃食物 |
| 照片压缩 | 拍照后自动压缩至400px、JPEG 0.5质量，节省存储 |
| IndexedDB照片存储 | 食物照片存入本地数据库，支持查看大图 |
| 运动量调节 | 4级运动量（偏低/正常/稍高/高）影响每日推荐热量 |
| 目标设置 | 减脂（-500kcal）/维持/增肌（+300kcal）影响推荐热量 |
| 外观切换 | 跟随系统 / 浅色 / 深色 手动切换 |
| 离线使用 | Service Worker缓存，添加到主屏幕后完全离线可用 |
| 数据导出/导入 | JSON格式备份恢复所有数据（含照片标记） |
